<%= form_with(model: [board, comment], local: true) do |form| %>
  <%= form.text_area :content, placeholder: "コメントをここに", class: "form-control" %>
  <%= form.submit "コメントする", class: "btn btn-primary" %>
<% end %>
