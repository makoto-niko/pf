<% board.comments.each do |comment| %>
  <div class="comment">
    <p><%= comment.user.username %>: <%= comment.content %></p>
    <% if current_user.id == comment.user_id %>
      <%= link_to "削除", [board, comment], method: :delete, class: "btn btn-danger" %>
    <% end %>
  </div>
<% end %>
